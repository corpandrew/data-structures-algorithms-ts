import { defineConfig } from 'vitest/config'; // Import defineConfig from 'vitest/config'

export default defineConfig({ // Use defineConfig from 'vitest/config'
    test: {
        environment: 'node', // or 'node'
    },
});